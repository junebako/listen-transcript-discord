# プライバシーポリシー

「LISTEN 文字起こし編集 Discord 通知」(以下、本拡張機能) のプライバシーポリシーです。

## 収集・送信するデータ

本拡張機能は、LISTEN (listen.style) 上で行われた文字起こしの編集内容 (変更前後のテキスト、エピソードタイトル、エピソード URL、タイムスタンプ) を検知し、**ユーザー自身が設定した Discord Webhook URL** へ送信します。

- 送信先はユーザーが番組ごとに明示的に登録した Webhook のみです。登録していない番組の編集内容はどこにも送信されません
- 開発者のサーバや第三者のサーバへデータを送信することはありません
- アクセス解析やトラッキングは行いません

## 保存するデータ

以下の設定情報を Chrome の同期ストレージ (`chrome.storage.sync`) に保存します。

- 番組ごとの Discord Webhook URL
- 通知に表示する任意の表示名

これらはユーザーの Chrome プロファイル内 (および Chrome の同期機能) にのみ保存され、開発者が取得することはできません。

## データの第三者提供

行いません。ユーザーが設定した Discord Webhook への送信がすべてです。

## 変更

本ポリシーを変更する場合は、このページを更新します。

## 連絡先

不明点は GitHub リポジトリの Issue でお問い合わせください。
